# Schedule Command — Backlog

**Single source of truth for all outstanding work.** Update on every session that
completes, defers, or discovers an item. Status values: `Open`, `In Progress`,
`Blocked`, `Done` (move Done items to the Completed Log at the bottom within a
session or two).

Created: 2026-06-17 — first entries are the 6 adjacent findings from the
`billing_forecast_integration.md` plan↔audit loop (rounds 1–3). "Adjacent" =
surfaced by, but not caused by, the billing-forecast work; filed here rather than
folded into the build per audit scope discipline.

## Tier definitions

- **T0** — Drop everything. Active prod breakage / security incident.
- **T1** — This session. High-severity-and-likely × low-cost.
- **T2** — This sprint. High strategic leverage, or unblocks T1/T2 work.
- **T3** — When convenient. Low-severity bugs, refactor, polish.
- **T4** — Only if forced. Needs a re-trigger before it can move.

---

## Active

| ID    | Tier | Status | Item                                                  | Source                          | Notes                                                                                                                                                                                                                                                                                                |
|-------|------|--------|-------------------------------------------------------|---------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| ADJ-1 | T2   | **Verified (forecast-safe) 2026-06-18** | Two retention conventions coexist on `invoices`       | billing-forecast audit r1 (Med) | **Repo: sales-command.** Active `retention_*` (`20260420170000`/`20260601120000`) vs legacy `retainage_*` (`20260416175646`). If any invoice carries values in the legacy set, net-of-retention forecast math reads the wrong/null column and miscomputes inflow. **Fix:** confirm the `20260505140000` pay-app backfill populated `retention_*` (not `retainage_*`) across live invoices, then treat `retention_*` as canonical suite-wide. Load-bearing for the forecast's net math. **VERIFIED at billing-forecast build start (T3, 2026-06-18, service-role count):** of 70 live invoices, 10 carry `retention_amount>0` and only 2 also carry legacy `retainage_amount>0` — and BOTH of those also have a populated `retention_amount` (id 10004: retainage 1561.64 = retention 1561.64; id 10024: retainage 2154.53 but retention 3211.25 = correct 5%×amount). **Zero live invoices have retainage without a populated, canonical-correct retention_amount**, so the forecast's `− COALESCE(retention_amount,0)` never counts gross. `retention_*` confirmed canonical suite-wide for the forecast. Residual (out of scope): 10024's stale legacy `retainage_amount` disagrees with its retention — data-quality curiosity; nothing in scope reads `retainage_*`. |
| ADJ-3 | T2   | **Done (shipped)** | `/billing` redirects carry no tab param               | billing-forecast audit r1 (Low) | **DONE — verified 2026-06-30 (Loop #38):** `Jobs.jsx` + `JobsPicker.jsx` already route to `/billing?tab=worklist`. **Repo: sch-command. Coupled to the billing-forecast build** — builder should pick this up when `/billing` becomes two tabs. `TAB_REDIRECTS` (`Jobs.jsx:18-19`) and the Ready-to-Bill tile (`JobsPicker.jsx:75` `goBilling`) route to `/billing` with no tab; once it's Worklist + Forecast they land on the undefined default. **Fix:** name the default tab and deep-link the RTB tile (e.g. `/billing?tab=worklist`).                                              |
| ADJ-2 | T3   | Open   | Forecast stale until manual reload                    | billing-forecast audit r1 (Low) | **Repo: sch-command (reads sales-command data).** No Supabase realtime subscription on `invoices`, so "Paid removes the row" and new-invoice inflow only reflect on next manual reload — §4.6's "now automatic" overstates. **Fix:** add a realtime subscription on `invoices` (or a refresh affordance) so the forecast updates without a manual reload. `Billing.jsx` load is a one-shot `useEffect` (~line 101).                                              |
| ADJ-4 | T3   | **Closed (2026-07-01, Loop #39)** | `terms_override` redundant in §4.2 fallback COALESCE  | billing-forecast audit r2 (Low) | **CLOSED — `expectedPayDate()` step-3 COALESCE dropped `termsOverride` (`billingForecast.js:171`); doc comment updated. Provably dead: step 1 returns when `(termsOverride && sent)` and line 171 is inside `if (sent)`.** Original: **Repo: sch-command.** In the expected-pay-date resolution, `terms_override` was both step 1 (wins over `due_date`) AND inside the step-3 fallback COALESCE; the step-3 occurrence was dead but invited a precedence inversion. **Fix:** drop `terms_override` from the step-3 COALESCE — leave it only as step 1. |
| ADJ-5 | T3   | Open   | JobDetail billing history stale post-cutover          | billing-forecast audit r2 (Low) | **Repo: sch-command.** Once `billing_log` writes stop, the per-job billing history (`JobDetail.jsx:76`, read-only `billing_log` consumer) shows only frozen pre-cutover percent rows with no new entries — misleading if users expect new invoices there. **Fix:** label it "legacy progress log; invoices live in [new tool]", or point the history at the invoice source.                                              |
| ADJ-6 | T3   | Open   | Past-due bucket is gross-of-partials                  | billing-forecast audit r3 (Low) | **Repo: cross-repo** (column is sales-command `invoices`; consumed by sch-command forecast). `invoices` has no `amount_paid`/`balance_due` column (`paid_at` is binary), so an invoice partially paid out-of-system still shows full net in the highest-trust past-due bucket, overstating collectable inflow. **Fix:** add `invoices.amount_paid`/`balance_due` and subtract in the bucket. **Interim (in v1):** keep the gross-of-partials label so the number isn't trusted as net-collectable. **CONFIRMED REAL by Chris (2026-06-18 walkthrough):** distinguish partial BILLING from partial PAYMENT. Partial *billing* (e.g. Steve's Collision call_log 3609: $11,768 contract, one invoice = $5,884 = 50%) is fully tracked via the invoice amount — NOT this gap. The gap is partial *PAYMENT* of a sent invoice, which happens for **non-GC customers** (customer pays part, notified or not); pay-app GCs pay in full so they're unaffected. **Fix scope:** `amount_paid`/`balance_due` matters for regular customer invoices. **Also reword the on-screen caveat** to plain language about partial *payment* (current wording confused billing vs payment). |
| ADJ-7 | T3   | Open (narrowed)   | Retire remaining read-only `billing_log` reader        | billing-forecast build (T3, 2026-06-18) | **Repo: sch-command. Deferred follow-up per plan §7** (billing_log kept READ-ONLY, not deleted; full reader retirement is post-proof, reversible). **Closed after T4 buildvsplan (2026-06-18):** the Billing Report export (`exports.js`) was RETIRED (printed frozen percent placeholder); the `jobs.billed_to_date` percent input in `Schedule.jsx` was REMOVED (N10). **Still deferred:** the **JobDetail billing-history tab** (`JobDetail.jsx:76`) read — kept READ-ONLY per §5.2a #4 / §7 (shows frozen legacy rows, won't crash). **Also (record):** plan §5.2a's 9-site census UNDERSTATED the footprint — `billing_log` was also woven through `StageJobCard` (banner warning + management 'BILLED %' tile), `Jobs.jsx` `urgencyScore`, and `Staged/OnHoldCardList` forwarding; all cleaned in v1. **Fix (remaining):** re-point JobDetail history at the invoice/worklist source (or retire), then drop `billing_log` once the new surface is proven. |
| ADJ-a | T3 | Open | `loadJobs()` unpaginated — silent row-drop past 1000 jobs | billing-redesign audit r1 (Low, adjacent) | **Repo: sch-command.** `loadJobs()` (`queries.js:129-137`) does a raw `supabase.from('jobs').select(...)` with no `.range()` — violates CLAUDE.md "PostgREST caps at 1000 rows — paginate with `.range()`." `buildBillingSurface()` (`billingForecast.js:268`) iterates this jobs set, so past 1000 jobs the billing gate silently drops rows. **Pre-existing** (reverting the deposit gate leaves it), capped **Low** — HDSP nowhere near 1000 jobs. **Fix:** add `.range()` pagination per CLAUDE.md. Not a Phase-1 blocker. |
| ADJ-b | T3 | **Closed (dup of ADJ-2)** | Billing load one-shot — no realtime, stale until manual reload | billing-redesign audit r1 (Low, adjacent) | **CLOSED 2026-06-30 (Loop #38) as a duplicate of ADJ-2** — same "billing load is one-shot, no realtime subscription on `invoices`." Tracked as ADJ-2 (Phase 6). **Repo: sch-command (reads sales-command data).** `Billing.jsx` load is one-shot (`loadData` in `useEffect`, `Billing.jsx:41-55`) with no Supabase realtime subscription. After Sales ships a deposit invoice in another app/tab, Schedule's worklist won't flip to Partially Billed until manual reload. **Real fix = ADJ-2 (Phase 6, out of scope).** **POINT-AT demo gotcha:** the demo script must reload between the Sales-send step and the Schedule-check step or it shows stale data. Capped **Low** (solo users + realtime out of scope this phase). |
| BF-1 | T3 | **Done (verified in code 2026-07-08)** | Header + "Back" button on every billing screen | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | **DONE — confirmed already shipped (not a stale open item).** Both billing screens carry a `fc-header` with `← All jobs` (→ `/jobs`) + title: worklist picker (`BillingPicker.jsx:50-52`, "Billing") and forecast (`Forecast.jsx:42-44`, "90-Day Forecast"). Card drill-in also has its own header + back: `← All billing cards` + category title + count (`BillingPicker.jsx:100-105`). Covers Worklist + Forecast + drill-ins per the original ask. Label landed as "All jobs". |
| BF-2 | T3 | Open | Billing time filters (day/week/month/quarter/year + custom) | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | Add time-period filter to the billing header: day/week/month/quarter/year presets + custom date-range picker. Mirror the `Jobs.jsx` shell-filter pattern. **The header date IS the active filter** (ties to BF-8: it scopes the "done" sections, not the action pile). |
| BF-3 | T3 | **Core built 2026-07-01 (Loop #39) — pending /buildvsplan + smoke** | Card-picker layout = billing-state categories (the heart of the redesign) | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | **CORE BUILT (Loop #39, not yet smoke-tested):** `/billing` worklist tab is now a 4-card picker by billing state (`BillingPicker.jsx`: Ready to Bill / Partially Billed / Billed Complete / Pay Apps + TOTAL TO BILL header) → drill into a purpose-built billing card (`BillingCard.jsx`, Option B — borrows sjc-* design language, NOT a StageJobCard fork): stage banner + billing badge + CONTRACT/BILLED/REMAINING bubble + BILLING tab (Hold/N-B/terms/notes). Cards keyed on derived fields via `billingCardKey()`/`billingBadge()`; On-Hold greyed via `heldSales`; banner via `productionStage`. `BillingWorklist.jsx` removed. **Forecast relocation DONE 2026-07-02 (Loop #39):** two-tab shell killed; `/billing` is worklist-only; 90-Day Forecast moved to its own screen (`/billing/forecast`, `views/Forecast.jsx`) reached from a new home-screen "90-Day Forecast" card in JobsPicker's Job Management Stages. TOTAL TO BILL now clickable → drill-in of still-owed jobs as billing cards; identity bubbles fixed (two-row, no truncation). **Still open (separate rows / later):** BF-1 header+back, BF-2 time filter, BF-8 done-pile scoping, BF-5 row→Sales, BF-6 forecast restyle, "All" card, `pay_app_billing_day` due-date alert. **SPEC LOCKED 2026-06-30 (Loop #38) — see `docs/plans/billing_redesign_buildorder.md` "Phase 2 card-mapping decision":** Billing screen = **4-card picker by billing state** — Ready to Bill / Partially Billed / Billed Complete / **Pay Apps** (+ optional All). Reuse `JobsPicker` shell + `StageJobCard` cards (banner = production, banner-right = billing badge, money bubble CONTRACT/BILLED/REMAINING, manual controls → BILLING tab). Home-screen "Ready to Bill" card → renamed "Billing" = entry point. **90-Day Forecast relocates** to its own home-screen "Job Management Stages" card (kills the two-tab shell). On Hold greyed inside its billing card. Pay Apps card ships off `requires_pay_app`; monthly due-date alert = fast-follow (new `pay_app_billing_day` field). Original note: Make the worklist a card-picker like `JobsPicker` (Chris's "3 simple screens beats 1 busy screen"): one card per category w/ count, click to drill into just those jobs, + an "All" card. **Merges the lifecycle status-model Delta** — cards should BE the categories: **Not Started / In Production / Production Complete / Partially Billed / Fully Billed / All**. "Production Complete" can be built now off the existing `Complete` status (Field Command "Job Complete" auto-trigger is a later cross-repo automation). Adds a **"Job not started"** manual flag (distinct from Hold). **Clean rows** (scan-only: job, amounts, status, terms as a read-only label); **move manual controls (Hold/Nothing-to-bill/terms-override/notes) to the drill-in** (they're Schedule-owned; the row click now goes to Sales, BF-5). Rename "Invoice Sent"→"Partially Billed", "All Ready Billed"→"Fully Billed". |
| BF-4 | T3 | **Done (shipped, smoke-verified)** | Deposit feature — end-to-end (cross-repo: Sales + Schedule) | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | **SHIPPED Loops #36–#37, smoke-verified 2026-06-30 (Loop #38, Chris).** Landed with a **revised data model:** deposit fields on `call_log` (one-field, migrations `20260620120000`+`20260621120000` in `command-suite-db`), job→invoice via `call_log.deposit_invoice_id` pointer, `invoices.type='deposit'`, MATERIALS DEPOSIT INVOICE badge, Schedule Parked-gate surfacing. Original spec below (superseded on data-model): **No deposit data exists** anywhere today (current "Deposit due" is an unbacked guess). Real flow: (1) Sales Command proposal gets a **"Deposit required" checkbox + manually-entered deposit AMOUNT** (source of truth). (2) Surfaces in Schedule's billing worklist **only once the job is SCHEDULED (has dates)** — not just sold ("for now"). (3) Amount shows at billing as a **SUGGESTED, editable** figure. (4) Carries onto the invoice → labeled a **"deposit invoice"**. (5) Once sent, drops to Partially Billed. (6) Keeps its unique invoice ID for traceability. **Near-term (independent):** relabel the unbacked "Deposit due" to "Not billed yet"/"Needs first bill". |
| BF-5 | T3 | Open | Clickable worklist rows → Sales Command job (new tab) | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | Row click → open `salescommand.app/calllog/<call_log_id>` in a **new tab** (Schedule's JobDetail has NO invoices/proposals — verified; Sales does). Every row already carries `call_log_id`. New tab solves "getting back" (Schedule stays open). Don't steal the click from the drill-in manual controls. Confirm prod Sales host. |
| BF-6 | T3 | Open (**narrowed**) | Card/bubble design restyle + clickable forecast rows | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | **NARROWED 2026-06-30 (Loop #38):** worklist-row restyle folded into BF-3 (StageJobCard reuse). BF-6 now = **Forecast per-week drill-down restyle only** (plain table today) + clickable forecast rows → Sales job (new tab). No plain table/list views — use the suite's card/bubble style. **Design reference: Sales Command's Proposals + Invoices list screens** (`~/sales-command`). Applies to the Forecast per-week drill-down (currently a plain table) + the worklist rows (pairs w/ BF-3). Each forecast invoice row → clickable → Sales job (new tab, like BF-5); default `/calllog/:id`, OPEN whether invoice rows should deep-link `/invoices/:id` instead. |
| BF-7 | T3 | Open | Past Due: AR aging bands + days-overdue + color | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | Per invoice show "X days overdue" (today − expected pay date), sort oldest-first. Subdivide Past Due into standard AR bands w/ $ subtotals, color by risk: 1–30 (yellow), 31–60 (orange), 61–90 (red), 90+ (dark red). "Current" = not-yet-due → stays on the upcoming/forecast side, NOT in Past Due. Also **reword the gross-of-partials caveat** to plain English about partial PAYMENT specifically (see ADJ-6 — partial *billing* is tracked; partial *payment* on non-GC invoices is the real gap). |
| BF-8 | T3 | Open | Time-scope worklist per section + scheduled-only population (defines what the worklist IS) | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | **ACTION pile** (needs billing / unbilled balance) → NOT date-scoped, show ALL still-owed work so nothing old slips. **DONE pile** (Partially/Fully Billed) → scoped to the active window by **invoice `sent_at`** (NOT `paid_at`): rolling "what I billed this week" snapshot, stays short. **Population gate:** EXCLUDE any job WITHOUT schedule dates (`scheduled_end`/`end_date` or `partial_bill_date`) — deposits included (a deposit shows only once scheduled). Ties to BF-2 (header date = active window). |
| BF-9 | T3 | Open | Forward/back time nav = upcoming billing calendar | billing-forecast v1 validation walkthrough (T3, 2026-06-18) | Step the header window forward/back to see upcoming billing actions ("when should I be invoicing in the future"). Predict future-billable from `jobs.scheduled_end`/`end_date` (work finishes → completion bill) + `jobs.partial_bill_date` (user-set next-bill date). **Keep DISTINCT from the Forecast tab:** Forecast = cash IN (payments on sent invoices); this = invoices OUT (upcoming billing). Limit honestly in UI: only jobs WITH a scheduled date can be placed on a future week (undated → "needs billing / unscheduled"). Builds on BF-2/BF-8. |
| BF-10 | T3 | **Done (shipped to prod 2026-07-08)** | Billing-card notes: pop-up modal instead of truncating inline input | BF-3 tweak walkthrough (2026-07-08) | **DONE — merged to main + live on schedulecommand.com (commit `2dd93cb`).** The billing card's `chris_notes` was a single-line `<input>` crammed in the controls row; long notes truncated to "We had to go back to…" and couldn't be read/edited. Replaced with a compact **Notes** button (green dot + truncating preview when a note exists) → pop-up modal (app `.mbg`/`.mdl` shell) with a 5-row textarea + Save/Cancel. Save path unchanged (`onFlag(jobId, 'chris_notes', v)`; empty → `null`). `BillingCard.jsx` + `App.css`. Preview-verified before merge. |
| BF-11 | T3 | **Done (shipped to prod 2026-07-08)** | Field SOW materials picker reuses Sales Material Memory (materials_catalog) | Planning-tab walkthrough (2026-07-08) | **DONE — merged to main + live (commit `42797b8`).** The Field SOW "+ Add Custom Material" only offered this job's WTC materials + a blank custom entry; it never read `materials_catalog`, so the shared material DB wasn't reusable in Schedule. Added `loadMaterialsCatalog()` (`queries.js`, read-only + RLS-scoped + paginated + Sales' name/kit dedupe); `CardSowModal` loads it once → every `FieldSowBuilder`; `DayMaterials` dropdown gains a searchable **FROM MATERIAL MEMORY** section beside the kept proposal quick-add + custom option. **Read-only reuse (scope decision):** custom materials stay SOW-local with an inline note directing the user to add them in Sales for reuse; Schedule never writes the Sales-owned catalog. Preview-verified (catalog populates under Schedule auth). |
| DMS-1 | T2 | **Plan (Phase 0 pending) — 2026-07-08** | Daily Material Schedule job ticket — cross-repo (Sales SOW + Schedule SOW/MTRL) | Warehouse-form pivot (2026-07-08) | **Target:** reproduce the crew/warehouse "DAILY MATERIAL SCHEDULE" job ticket (built in Claude Web, crew loves it). **Reference PDF:** `~/Desktop/6618 - Lakes Crossing - Material Schedule.pdf` — COPY into the repo (never leave on Desktop). **Structure:** page-1 **Material Order Summary** (per-material totals across all days) + **per-day cards** (crew·hours·**sqft·linear ft**·WTC, work-to-complete tasks+%, **scope notes**, materials w/ specs) + print/sign frame (logo, prepared-by, lead+sales signatures). **CORE ARCHITECTURE [PROPOSED — ratify in plan]:** material application specs (kit, mils, coverage, mix time/speed, cure, unit) live on **`materials_catalog`** (Material Memory) so they flow Sales bid → Schedule field SOW → ticket; per-job/day override allowed. This is why the **Sales Command SOW process/outputs must change too**. **DATA GAPS:** (1) `materials_catalog` add spec cols; (2) `field_sow` day add `scope_notes`, `sqft`, `linear_ft` (jsonb, no migration); (3) materials gain a **`task_ref`** (link each material → its task). **UI DECISIONS [LOCKED this session]:** (a) day-card header = day_label title + **TASK count**; DROP the redundant "DAY X OF 7" badge (footer keeps "N days scheduled" for total). (b) **Work to Complete** = one row per task, **"TASK N"** label + colored **% badge** (100%=green, partial=amber); handles split-day (e.g. 50%/50%). (c) Materials tagged with a **TASK N chip** (Option A — flexible; blank if unassigned). **PHASES:** 0 plan doc `docs/plans/daily_material_schedule.md` + field-ownership matrix (bid vs field) + update Command Suite data contract; 1 `command-suite-db` migrations; 2 Sales Command SOW (specs on bid materials + scope/measurements + Send-to-Schedule stamp); 3 Schedule SOW builder (scope_notes + sqft/lf + specs inherit from catalog); 4 output (MTRL = Order Summary + per-day ticket + Print PDF); 5 backfill + retire legacy materials-upload; verify vs job 6618. **Plan-mode work (cross-repo + schema) — needs its own planning terminal + `/erd-start`.** |
| BF-12 | T3 | **Parked 2026-07-08 (superseded → DMS-1)** | MTRL modal SOW rollup + order-status bridge | Planning-tab walkthrough (2026-07-08) | **PARKED on branch `feat/mtrl-sow-rollup` (pushed, NOT merged).** Rebuilt the MTRL modal as a per-material rollup of `field_sow` day materials (summed qty + per-day breakdown + specs) with order status bridged to the `materials` table by name (table has no kit_size col — see commit `2233d9c`). **Superseded by DMS-1:** the Daily-Material-Schedule data-model change reshapes this, so do NOT merge — its `rollupSowMaterials()` logic is the seed for DMS-1 **Phase 4** (Material Order Summary). Preview-only. Fold in during Phase 4, then delete the branch. |
| SJC-1 | T3 | **Display cap shipped 2026-07-01 (Loop #39); real fix still deferred → Field Command** | ACTIVE banner "day X of Y" has no cap past end date (e.g. "day 129 of 5") | Loop #38 walkthrough (2026-06-30) | **DISPLAY CAP SHIPPED (Loop #39):** day number now `Math.min(totalDays, Math.max(1, elapsed+1))` so a stale past-end job reads "day 5 of 5", never "day 129 of 5". The real fix (elapsed from first clock-punch + "Nd overdue" reframe) still needs Field Command. **Repo: sch-command. `StageJobCard.jsx:137-139`.** `dayText = day {elapsed+1} of {totalDays}` where `totalDays = end−start+1` and `elapsed = today − start_date`; `Math.max(1, elapsed+1)` floors at 1 but never caps at `totalDays`, so a job past its end reads e.g. "day 129 of 5". The numbers are correct math on a stale job (see SJC-2), just uncapped. **FIX DEFERRED UNTIL FIELD COMMAND IS DONE:** the real fix changes the elapsed math from `start_date` to the **first time-clock-punch date** (Field clock-in) and reframes past-end as "Nd overdue". No punch date exists until Field ships. **Note:** `StageJobCard` is reused for the billing worklist (BF-3), so this quirk will appear there too. |
| SJC-2 | T3 | Open (**deferred → Field Command**) | Stale jobs stuck in ACTIVE — production status never advances to Complete | Loop #38 walkthrough (2026-06-30) | **Repo: sch-command (data / lifecycle).** Jobs (mostly old test rows — ZZ TEST / SOW-smoke) sit in ACTIVE with months-old `start_date`, never moved to Complete — which is what surfaces the "day 129 of 5" number (SJC-1). Root = the production-signal gap: `In Progress → Complete` is manual today; nothing auto-closes a job past its end. **FIX DEFERRED UNTIL FIELD COMMAND IS DONE:** Field clock-out / job-complete auto-advances status and overdue-but-open jobs get flagged; ties to the same start-date → first-clock-punch change as SJC-1. **Interim:** manually mark the stale test jobs Complete to clear the noise. |
| SEC-1 | T2 | Open | `billing_worklist` writes tenant-scoped but NOT role-gated (Admin gate is client-only) | Loop #39 security review (2026-07-01) | **Repo: command-suite-db (RLS migration).** The billing card's manual controls (Hold / Nothing-to-bill / terms-override / notes) write `billing_worklist` via `setBillingWorklistFlag`. The UI gates on `user.role === 'Admin'` (`Billing.jsx:21`) but that is **client-side only**. Read the live policies (`command-suite-db/supabase/migrations/20260618120000_billing_worklist_create.sql`): all 4 ops are **tenant-scoped** via the `jobs → call_log.tenant_id` chain (cross-tenant isolation ✅ solid, read + write), but **none is role-gated** — so any *same-tenant* authenticated user (e.g. a Sales rep) can INSERT/UPDATE/DELETE billing overrides by bypassing the browser. **Inconsistent with the standing rule** ([[feedback_role_gating]] "Sales uploads docs only; Admin/Manager configure money") and with the sibling money tables `billing_schedule` / `billing_schedule_lines` / `materials_catalog`, which DO gate writes on `public.is_admin_or_manager()` (`20260416210000_role_aware_money_rls.sql`). **Pre-existing** (table created 2026-06-18, before the BF-3 build — not introduced by Loop #39; the card just reuses the same write path). **Severity low** (same-company only, small team) but it's a money-write authz gap. **Fix:** add `AND public.is_admin_or_manager()` to `billing_worklist` INSERT/UPDATE/DELETE `WITH CHECK`/`USING`, matching `role_aware_money_rls`; new migration in `command-suite-db`. `customers` embed (C1) = no new exposure, tenant-scoped read already governs it (verified). Do before a paying customer's non-Admin staff use the billing screen. |

---

## Completed Log

(none yet)
